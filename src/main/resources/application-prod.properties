# DataSource Configuration
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:oepp_db}?useUnicode=true&characterEncoding=utf8&useSSL=true&serverTimezone=Asia/Taipei
spring.datasource.username=${DB_USERNAME:oepp}
spring.datasource.password=${DB_PASSWORD:3hDodFKy6X}

# HikariCP Configuration
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=10

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Logging Configuration
logging.level.root=WARN
logging.level.com.nkust.oepp=INFO

# CORS Configuration (生產環境)
# 允許的前端域名，多個域名用逗號分隔
cors.allowed-origins=https://edu.nkust.edu.tw,http://edu.nkust.edu.tw

